{
  "name": "doubleshot-monorepo",
  "private": true,
  "packageManager": "pnpm@7.0.1",
  "description": "A solution to save effort",
  "author": "ArcherGu (https://github.com/ArcherGu/)",
  "keywords": [
    "electron",
    "vite",
    "nestjs"
  ],
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@lightwing/eslint-config": "^0.0.1",
    "@types/node": "^17.0.35",
    "eslint": "^8.16.0",
    "lint-staged": "^12.4.1",
    "rimraf": "^3.0.2",
    "simple-git-hooks": "^2.7.0",
    "tsup": "^6.1.0",
    "typescript": "^4.6.4"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,tsx,vue,md}": [
      "eslint --fix"
    ]
  }
}
